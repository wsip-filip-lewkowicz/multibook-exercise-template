{
  "name": "multibook-exercise-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:test": "vite build --mode development",
    "preview": "vite preview",
    "build:zip": "rm -rf dist && vite build && cd dist && zip -r ../dist.zip . && cd .."
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "vite": "^7.2.4"
  }
}
