{
  "name": "multibook-exercise-template",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:zip": "vite build && cd dist && zip -r ../dist.zip . && cd .."
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.8",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "@phosphor-icons/web": "^2.1.2"
  }
}
